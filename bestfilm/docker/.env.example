# ⚠️⚠️ DOCKER COMPOSE-ИЙН BUILD ХУВЬСАГЧ
#
# ЯАГААД ТУСДАА ФАЙЛ ВЭ: `docker-compose.yml`-ийн `args:` дэх
# `${...}` нь ОРЧНЫ хувьсагчаас уншина — `env_file` нь зөвхөн
# RUNTIME-д (container дотор) үйлчилнэ, BUILD үед хүрэхгүй.
#
# ⚠️ `NEXT_PUBLIC_*` нь BUILD үед код руу ШИГДДЭГ тул энд ЗААВАЛ.
#    Хоосон бол Next.js нь `new URL('')` дуудаж «Invalid URL»
#    алдаагаар build УНАНА (бодит алдаа).
#
# Хэрэглэх:
#   cp docker/.env.example docker/.env
#   docker compose -f bestfilm/docker/docker-compose.prod.yml -p bestfilm build

# ⚠️ R2 — BestTV-тэй ЯГ ИЖИЛ (кино нэг санд, зай 2 дахин эзлэхгүй)
NEXT_PUBLIC_ASSETS_URL=https://assets.besttv.us

# ⚠️⚠️ Чат — BestTV-ИЙН WEBHOOK-ЫГ ХУВААЛЦАНА (`bestfilm-chat` БИШ).
#
# БОДИТ АЛДАА (2026-09-08): `bestfilm-chat` гэсэн webhook n8n дээр
# ОГТ БАЙХГҮЙ байсан тул BestFilm-ийн вэб чат чимээгүй ажиллахгүй байв.
#
# ⚠️ Тусдаа workflow ҮҮСГЭХГҮЙ («чат НЭГ эх сурвалж» дүрэм) — widget нь
# биедээ `site: "bestfilm"` илгээж, n8n түүнийг `/chat/ingest` болон
# `/titles/search` руу `x-site` толгойгоор дамжуулна.
NEXT_PUBLIC_CHAT_WEBHOOK_URL=https://bot.digitalger.mn/webhook/besttv-chat

# ⚠️ NextAuth — BestFilm-ийн домэйн (OAuth callback энэ дээр тулгуурлана)
NEXTAUTH_URL=https://bestfilm.net

# ⚠️⚠️ Хуваалцсан пакетын зам — ОРЧНООР ӨӨР:
#     локал: besttv/shared
#     VPS:   BestTV/shared   ← ТОМ үсэг
SHARED_DIR=besttv/shared

# ⚠️⚠️ САЙТЫН ХАЯГ — canonical / og:url / sitemap / robots.
#    Тохируулаагүй бол `metadataBase` нь localhost болж FB/Twitter дээр
#    хуваалцахад зураг ХООСОН гарна.
#    ⚠️ Төгсгөлд `/` БИЧИХГҮЙ (код өөрөө хасдаг ч давхардуулахгүй).
SITE_URL=https://bestfilm.net
NEXT_PUBLIC_SITE_URL=https://bestfilm.net
