# Техникийн тэмдэглэл — дахин алдахаас сэргийлэх

Олон удаа алдсаны эцэст олсон, кодоос харагдахгүй мэдлэг.

---

## ⚠️ `loading.tsx` нь `notFound()`-ыг эвдэнэ

**Илэрсэн:** 2026-08-09 · **4 оролдлогын эцэст**

### Шинж тэмдэг

`notFound()` дуудагдаж `not-found.tsx` рендерлэгддэг **мөртлөө**
HTTP статус **200** үлддэг. Google эвдэрсэн холбоосыг индексжүүлнэ
(soft 404).

### Шалтгаан

Route-д `loading.tsx` байх нь Next-д **streaming** идэвхжүүлдэг —
хариуны толгойг ЭРТ илгээх тул `notFound()` дараа нь статусыг
өөрчилж чадахгүй.

### Бүтээгүй оролдлогууд

| # | Юу хийсэн | Үр дүн |
| --- | --- | --- |
| 1 | Page-д `notFound()` нэмсэн | ❌ 200 |
| 2 | `generateMetadata`-д ч нэмсэн | ❌ 200 |
| 3 | Route-ийн дэргэд `not-found.tsx` | ❌ 200 |
| 4 | **`loading.tsx` хассан** | ✅ **404** |

### Зөв шийдэл

`loading.tsx`-ыг устгахгүй — **нэр солиод `Suspense`-д ашиглана**:

```tsx
// loading.tsx → detail-skeleton.tsx (нэр солино)
export default function DetailSkeleton() { /* … */ }

// page.tsx
import DetailSkeleton from './detail-skeleton';

export default async function Page() {
  if (missing) notFound();           // ← статус ЭНД тогтоно
  return (
    <Suspense fallback={<DetailSkeleton />}>
      <DetailClient />                {/* streaming ЭНДЭЭС эхэлнэ */}
    </Suspense>
  );
}
```

Skeleton хэвээр харагдана, статус ч зөв.

### Нэмэлт: `notFound()`-ыг ХОЁР ГАЗАРТ

`generateMetadata` **ба** page component хоёуланд дуудна. Блог дээр
зөвхөн метадатад байхад 200 хэвээр байсныг хэмжсэн — Next аль шатанд
статусыг бэхлэхийг баталгаагүй.

### `notFound()` нь EXCEPTION шиддэг

`try` блок дотор дуудвал `catch` барьж аваад **хэзээ ч ажиллахгүй**:

```tsx
// ❌ БУРУУ
try {
  const res = await fetch(url);
  if (res.status === 404) notFound();   // catch залгина!
} catch { /* … */ }

// ✅ ЗӨВ — fetch дотор, шийдвэр гадна
let missing = false;
try {
  const res = await fetch(url);
  missing = res.status === 404;
} catch { /* сүлжээний алдаа — 404 БИШ */ }
if (missing) notFound();
```

### «Олдсонгүй» ба «сүлжээ унасан»-ыг ЯЛГА

Хоёулаа 404 болговол backend түр унахад **байгаа** кино Google-ийн
индексээс хасагдана (сэргэхэд буцаж орох нь удаан). Зөвхөн
`status === 404` үед л `notFound()`.

---

## ⚠️ `sitemap.ts` — build үед хоосон үүсдэг

**Илэрсэн:** 2026-08-09 · **3 оролдлогын эцэст**

`.next/server/app/sitemap.xml.body` нь **build үед** үүсээд
`max-age=0, must-revalidate` толгойтой тул Next түүнийг тогтмол
барина. Build үед backend хүрдэггүй (container хараахан асаагүй)
→ файл ҮРГЭЛЖ хоосон, container дахин үүсэх бүрд ижил.

| # | Юу хийсэн | Үр дүн |
| --- | --- | --- |
| 1 | Тохиргоогүй | ❌ 5 URL |
| 2 | `force-dynamic` + `revalidate` хамт | ❌ зөрчилдөнө |
| 3 | `revalidate` ганцаараа | ❌ 5 URL |
| 4 | **`force-dynamic` ганцаараа** | ✅ **142 URL** |

`export const revalidate` ТАВЬЖ БОЛОХГҮЙ — `force-dynamic`-тай
зөрчилдөнө. Кэшлэлт нь `fetch(..., { next: { revalidate } })` дээр.

---

## ⚠️ Каталогийн `limit` нь sitemap-д тохирохгүй

`titles.list()` нь `Math.min(60, limit)` — `?limit=1000` гэсэн ч 60
буцаана. Тэр хязгаар нь **хэрэглэгчийн каталогийг хамгаалдаг** тул
өсгөж БОЛОХГҮЙ.

Sitemap-д тусдаа хөнгөн endpoint: `/titles/sitemap` (зөвхөн
`slug` + `updatedAt`, `CARD_SELECT`-ээс 20 дахин бага).

---

## ⚠️ 18+ контент — ГУРВАН давхар хамгаалалт

`/adult` жагсаалтын хуудсыг `noindex` болгох нь ХАНГАЛТГҮЙ —
киноны дэлгэрэнгүй (`/movie/<slug>`) нь **тусдаа зам**.

1. Sitemap-аас хасах (`allSlugs`-д `NOT_ADULT`)
2. Хуудас өөрөө `robots: { index: false }` (гадны холбоосоор олдвол)
3. `robots.txt`-д `/adult`

---

## ⚠️ Prisma нь олон-олон холбоосын талбараар эрэмбэлж ЧАДДАГГҮЙ

`title.findMany({ orderBy: { genres: { order } } })` — **боломжгүй**.

Жанр доторх киног `TitleGenre.order`-оор эрэмбэлэхийн тулд
холбоосын хүснэгт талаас query хийнэ:

```ts
prisma.titleGenre.findMany({
  where: { genre: { slug } },
  orderBy: [{ order: 'asc' }, { title: { createdAt: 'desc' } }],
  select: { title: { select: CARD_SELECT } },
});
```

---

## ⚠️ Цувралын видео нь `Title` дээр БИШ, `Episode` дээр

`Title.streamStatus` нь SERIES-д **хэзээ ч `READY` болдоггүй**.
Бэлэн эсэхийг шалгахдаа:

```ts
MOVIE  → Title.streamStatus === 'READY'
SERIES → seasons.some(s => s.episodes.some(e => e.streamStatus === 'READY'))
```

Үүнийг мартвал 10 анги нь бүрэн бэлэн цуврал ч «тоглох боломжгүй»
гэж тоологдоно.

---

## ⚠️ Видеоны код өөрчилбөл `worker`-ийг ЗААВАЛ rebuild

**Илэрсэн:** 2026-08-09 (дахин)

`backend` rebuild хийхэд `worker` **шинэчлэгддэггүй** — тэдгээр нь
ТУСДАА container. HLS processor нь зөвхөн worker-д ажилладаг.

### Шинж тэмдэг

Кодыг зассан ч хуучин зан төлөв үргэлжилнэ. Бодит жишээ: ffmpeg
таймаутыг 180 мин → 8 цаг болгосон ч кино дахин *«180 минут
хэтэрлээ»* гэж унасан — worker 23 цагийн өмнөх код ажиллуулж байв.

### Шалгах

```bash
ssh root@62.238.47.2 'docker exec besttv-worker   grep -c "STALL_TIMEOUT_MS" /app/backend/dist/src/storage/video-hls.service.js'
# 0 гарвал → ХУУЧИН код
```

### Rebuild

```bash
ssh root@62.238.47.2 'cd /opt/BestTV/docker &&   docker compose -f docker-compose.prod.yml -p besttv build worker &&   docker compose -f docker-compose.prod.yml -p besttv up -d --force-recreate --no-deps worker'
```

⚠️ Явж буй ажлууд ТАСАЛДАНА — гэхдээ `attempts: 2` тул дараалалд
буцаж орно. Хөрвүүлэлт эхнээсээ дахин эхэлнэ (30+ мин алдагдал),
тиймээс боломжтой бол ажил цөөн үед хийнэ.

### Аль код worker-т нөлөөлдөг вэ

- `src/storage/video-hls.service.ts` (ffmpeg, таймаут)
- `src/modules/videos/video.processor.ts` (дараалал, диск шалгалт)
- `src/modules/videos/video-recovery.service.ts`
- Email/queue/BullMQ холбоотой бүх код

---

## 6. JWT нь НЭГ СЕКУНДЭД ИЖИЛ ТОКЕН үүсгэдэг (`jti` заавал)

**Шинж:** Хоёр төхөөрөмжөөс зэрэг нэвтэрхэд нэг л session бүртгэгдэнэ.
Лог: `Unique constraint failed on the fields: (tokenHash)`.

**Шалтгаан:** JWT-ийн `iat`/`exp` нь **секундын** нарийвчлалтай. Нэг
хэрэглэгч нэг секундэд хоёр удаа нэвтэрвэл payload (`{sub}`) ижил,
`iat` ижил → гарын үсэг ижил → **ЯГ ИЖИЛ ТОКЕН**.

**Үр дагавар (төхөөрөмжийн хязгаарт):**
- 2 дахь session бүртгэгдэхгүй → хязгаар огт ажиллахгүй
- Хоёр төхөөрөмж нэг мөр хуваалцана → нэгийг гаргахад нөгөө нь ч гарна

**Засвар:** refresh token-д `jti: randomUUID()` нэмнэ
(`auth.service.ts` → `signTokens`).

⚠️ Access token-д хэрэггүй (түүнийг DB-д хадгалдаггүй).

---

## 7. `/api/auth/sessions` нь NextAuth руу орж 400 өгдөг байв

**Шинж:** `This action with HTTP GET is not supported by NextAuth.js`

**Шалтгаан:** `next.config.ts` rewrite-ийн regex
`(?!signin|signout|callback|session|csrf|providers|error|bridge)` нь
**угтвараар** тааруулдаг тул `sessions` нь `session`-д таарч,
backend руу rewrite хийгдэхгүй NextAuth catch-all руу орно.

**Засвар:** үгийн төгсгөл шалгана —
`(?!(?:signin|…|bridge)(?:$|/))`

⚠️ Ирээдүйд `/auth/callbacks`, `/auth/providers-list` гэх мэт нэр
нэмбэл ижил алдаанд орно. Шинэ `/api/auth/*` зам нэмэх бүрд
`node -e` тестээр regex-ыг шалгах.

---

## 8. Cloudflare-ийн ард бодит IP авах

`req.ip` → `172.69.x.x` (Cloudflare-ийн сервер). Хэрэглэгчийн бодит IP
нь **`CF-Connecting-IP`** header-т байна.

Дараалал: `CF-Connecting-IP` → `X-Forwarded-For`-ын эхний утга → `req.ip`.

⚠️ Эрх шалгахад IP хэрэглэхгүй (гаднаас хуурамчаар илгээж болно) —
зөвхөн хэрэглэгчид ХАРУУЛАХ зорилготой.

---

## 9. Төхөөрөмжийн хязгаар (`MAX_DEVICES = 2`)

**Хаана:** `backend/src/modules/auth/session.service.ts`

| Зан төлөв | Тайлбар |
|---|---|
| Хязгаар | 2 (нэвтэрсэн төхөөрөмжөөр, зэрэг үзэж буй урсгалаар БИШ) |
| Хэтэрвэл | Хамгийн **удаан ашиглаагүй** нь автоматаар гарна |
| ADMIN | Хязгааргүй (админ панел + сайт хоёулангаас ордог) |
| DB алдаа | **Fail-open** — нэвтрэлт зогсоохгүй |
| Токен | SHA-256 хэшээр хадгална (цэвэр утгаар БИШ) |
| Rotation | `refresh` бүрт хуучин мөр устаж шинэ бичигдэнэ |

**Endpoint:** `GET /auth/sessions?rt=<refresh>`,
`DELETE /auth/sessions/:id`, `POST /auth/sessions/revoke-others`,
`POST /auth/logout`

⚠️ **`/auth/logout` заавал дуудагдана** — эс бөгөөс session мөр
30 хоног үлдэж хязгаарын байрыг дэмий эзэлнэ (хэрэглэгч 2 удаа
гараад орвол «дүүрсэн» болно).

⚠️ Багцын нөхцөлд «Олон төхөөрөмж» гэж бичиж БОЛОХГҮЙ — DB-ийн
`Plan.features` дээр «2 төхөөрөмж хүртэл» гэж засагдсан.

---

## 10. Цувралын `streamStatus` нь эцэг `Title` дээр ХЭЗЭЭ Ч READY болдоггүй

**Шинж:** 10/10 анги бүрэн хөрвүүлсэн атлаа карт дээр «Бэлтгэж байна».

**Шалтгаан:** SERIES-ийн видео нь `Episode` дээр. `Title.streamStatus`
нь `NONE` хэвээр үлддэг.

**Засвар:** `CARD_SELECT`-д `seasons.episodes.streamStatus` нэмээд
`TitleMediaHelper.decorate()` дотор тооцно (**бүх карт тэр функцээр
дамждаг** тул нэг газар засахад 14 дуудлага бүгд зөв болно).

Дүрэм: **нэг ч анги READY бол READY** (цуврал ангиараа гардаг тул
«бүгд бэлэн болтол хүлээ» гэж харуулах нь буруу).

⚠️ Админ талд (`titles-admin.service.ts`) ижил логик тусад нь бий —
нэгийг өөрчилвөл нөгөөг нь ч шалгах.

---

## 11. Урамшууллын систем

**Модел:** `Promotion` (4 төрөл), `PromotionPlan` (хамрах багц),
`PromotionRedemption` (хэн юу авсан)

### Үнэ бодох ДАРААЛАЛ (ЗААВАЛ дагах)

```
Багцын үнэ → УРАМШУУЛАЛ → КУПОН → Эцсийн үнэ
```

⚠️⚠️ Нийлбэрээр БОДОХГҮЙ: 30% + 20% = **44%** (50% биш). Урамшууллыг
эхэлж бодоод, купоныг ҮЛДСЭН дүн дээр тооцно.

⚠️ Энэ дараалал **4 газарт** давтагдана — нэгийг өөрчилвөл бусдыг ч:
1. `payments.service.ts` → `initiate` (QPay)
2. `payments.service.ts` → `purchaseWithWallet` (хэтэвч)
3. `bank.module.ts` → `initiate` (данс)
4. `pricing/page.tsx` (frontend харагдац)

Зөрвөл хэрэглэгч харсан үнэ ба бодит төлбөр таарахгүй.

### Хэд хэдэн урамшуулал тохирвол

`PromotionsService.valueOf()` нь хоногийг ₮ болгож (нэг хоногийн үнэ =
багцын үнэ / хоног) харьцуулаад **ХАМГИЙН АШИГТАЙГ** сонгоно.

### `promotionId` нь төлбөрт SNAPSHOT

⚠️ `initiate` үед тэмдэглэгдэнэ, `completePayment` нь түүнийг уншина.
Тэмдэглэхгүй бол: хэрэглэгч QR-аа 5 минут хүлээж байх зуур урамшуулал
дуусвал **хямдруулсан үнээр төлөөд бонусаа авахгүй**.

### Устгах хориг

`PromotionRedemption` бүхий урамшууллыг устгах боломжгүй (Cascade тул
түүх алдагдана). Оронд нь `isActive: false`.

### Хязгаарын хамгаалалт

- `PERCENT` нь 1-100 (DTO + service хоёуланд)
- `FIXED` хямдрал багцын үнээс их байвал доод хязгаар **100₮**
  (0₮ бол QPay нэхэмжлэл унана)

---

## 12. Дансаар шилжүүлэх

**Гүйлгээний утга:** `BTV-XXXX` — `ABCDEFGHJKLMNPQRSTUVWXYZ23456789`
(O/0, I/1 хассан, хэрэглэгч гараар бичдэг тул андуурахгүй)

⚠️ Хэрэглэгч энэ кодыг бичихгүй бол банкны хуулгаас **хэний мөнгө
болохыг таних БОЛОМЖГҮЙ** — модалд хамгийн тод байрлуулсан.

**Урсгал:**
```
initiate (PENDING + reference) → хэрэглэгч шилжүүлнэ
→ claim (bankClaimedAt + Telegram) → админ шалгана
→ approve → payments.completePayment() → эрх нээгдэнэ
```

⚠️ `approve` нь `completePayment` **ДУУДНА** — эрх нээх логикийг энд
дахин бичихгүй (урамшууллын бонус, имэйл, Telegram бүгд тэнд).

⚠️ 24 цагийн idempotent — товч 2 удаа дарахад 2 гүйлгээний утга
үүсгэхгүй (нэг нь мөнхөд PENDING үлдэнэ).

⚠️ Унтраалттай үед `publicSettings` нь дансны дугаарыг ОГТ буцаахгүй.

---

## 13. n8n Telegram — сүлжээний холболт

⚠️⚠️ `besttv-backend` болон `digitalger-n8n` нь **ӨӨР docker сүлжээнд**
байсан тул контейнерийн нэрээр хандаж чаддаггүй байв.

**Шийдэл:** `docker-compose.prod.yml`-д external network нэмсэн:
```yaml
n8n_net:
  external: true
  name: digitalger-n8n-network
```
`backend` болон `worker` хоёулаа энэ сүлжээнд нэгдэнэ.

⚠️ Гадаад IP (`62.238.47.2:5678`) **БҮҮ ХЭРЭГЛЭ** — тэр порт
интернэтэд нээлттэй тул хэн ч хуурамч webhook дуудаж болно.

**Env:** `N8N_WEBHOOK_URL=http://digitalger-n8n:5678`

**Webhook замууд:** `besttv-payment-paid`, `besttv-bank-transfer`,
`besttv-daily-report`, `besttv-video-failed`, `besttv-alert`

⚠️ Мэдэгдэл алдагдвал **үндсэн ажил зогсохгүй** (fail-open, 5с timeout).

### Өдрийн тайлангийн цагийн бүс

⚠️ Сервер UTC — `setHours(0,0,0,0)` нь Монголд 08:00 болно. `dayRange()`
нь UB (UTC+8)-аар өдрийн хилийг тооцно, эс бөгөөс орлого буруу өдөрт
хамаарна.

⚠️ Redis lock заавал — backend болон worker хоёул ижил cron ажиллуулдаг
тул тайлан 2 удаа илгээгдэнэ.

---

## 14. Дансаар төлөх — БҮРЭН систем (2026-08-13 шинэчлэл)

### Гүйлгээний утга: 6 ОРОНТОЙ ТОО

⚠️⚠️ `BTV-MPQD` хэлбэр БУРУУ байв — бодит хэрэглээнд:
- Банкны апп нь **утсан дээр** — үсэг бичихэд гар солино
- M/N, P/Q андуурагдана
- 8 тэмдэгт (зураастай) = алдаа гаргах магадлал өндөр

Одоо `482913` — тоон гараар шууд, OTP-той адил танил, 900,000
хувилбар. `randomBytes` (Math.random БИШ — таамаглах боломжгүй байх).

### Олон данс (`BankAccount` хүснэгт)

⚠️ Өмнө нь `Settings` JSON дотор НЭГ данс байсан. Хэрэглэгч
**ӨӨРИЙН банкнаас** шилжүүлэхэд шимтгэлгүй тул 2-3 данс өгөх нь
хөрвөлтийг шууд нэмэгдүүлнэ.

| Талбар | Тайлбар |
|---|---|
| `logoKey` | ⚠️ Логотой бол хэрэглэгч НЭГ ХАРЦААР олно |
| `iban` | ⚠️ **ТУСДАА талбар** — дансны дугаартай нэг мөрөнд нийлүүлбэл хэрэглэгч алийг нь хуулахаа мэдэхгүй |
| `order` | Жагсаалтын дараалал |

⚠️ IBAN-аас **зай автоматаар хасагдана** — зайтай хуулбал банкны апп
татгалзана.

⚠️ Төлбөртэй дансыг **устгах хориотой** (`Payment.bankAccountId` нь
SetNull тул түүх алга болно).

### Төлбөрийн баримт (screenshot)

`POST /bank/:id/receipt` — ⚠️ `admin/uploads` нь зөвхөн админд
нээлттэй тул хэрэглэгчид ТУСДАА endpoint. WebP 1200px шахна
(банкны SS нь 2-4 MB).

Админд 48px thumbnail → дарахад бүтэн дэлгэцээр (жижигээр дүн
уншигдахгүй).

---

## 15. Мэдэгдлийн систем (`Notification`)

⚠️⚠️ **ЯАГААД:** дансаар төлсөн хэрэглэгч «баталгаажсан уу?» гэдгээ
мэдэхгүй хүлээнэ. Имэйл хоцордог/спамд ордог, Telegram нь зөвхөн
админд. Сайт дээрх мэдэгдэл нь дэмжлэгийн дуудлагыг эрс багасгана.

- Navbar хонх + уншаагүйн тоо
- ⚠️ `GET /notifications/unread-count` — **зөвхөн тоо**, 60с polling.
  Бүтэн жагсаалт татвал хэрэггүй өгөгдөл байнга дамжина.
- `NotificationsService.create()` нь **`void`** — алдаа гарвал
  зөвхөн лог, үндсэн үйлдэл зогсохгүй

⚠️ `@Global` модуль — олон газраас дуудагдана (bank, payments,
subscriptions), import шаардвал шинэ газарт мартагдана.

---

## 16. Хэрэглэгчийн tracking («Түүх» таб)

⚠️⚠️ `UserAuditLog` хүснэгтэд нэвтрэлт, нууц үг солих, эрх өөрчлөх,
админы гар үйлдэл БҮГД бүртгэгддэг атлаа админ панелд **ОГТ
харагддаггүй** байв.

`GET /admin/users/:id` одоо нэмж буцаана:
- `auditLog` (40) — хэн/юуг/хэзээ/IP
- `sessions` (5) — нэвтэрсэн төхөөрөмж
- `notifications` (20) — уншсан эсэх
- `bankPayments` (10) — гараар баталгаажуулсан
- `promotionRedemptions` (10)
- `recentSearches` (15) — ⚠️ **0 үр дүнтэйг УЛААНААР** (контент
  нэмэх шууд дохио)
- `activity.totalSpent` / `playCount`

---

## 17. Хэтэвчийн навигаци

⚠️ pricing-ийн «Цэнэглэх» нь `/profile` руу явуулдаг байсан тул
хэрэглэгч ПРОФАЙЛ таб дээр бууж цэнэглэх хэсгээ өөрөө хайдаг байв.
Одоо `?tab=wallet`.

⚠️ Таб солиход URL нь `replaceState`-ээр шинэчлэгдэнэ (`push` БИШ —
эс бөгөөс буцах товч 10 удаа дарж байж сайтаас гардаг).

⚠️ Дансаар цэнэглэх товч нэмсэн — өмнө нь багцыг дансаар авч
чаддаг атал хэтэвчээ цэнэглэж чаддаггүй байв.

---

## 18. Хоёр мөрийн дүрэм — ЗӨВ томьёо

⚠️⚠️ `items.length > perRow` нь **БУРУУ**.

10 кино нь нэг мөрөнд багтахгүй (perRow=9) тул 2 мөр болдог — гэвч
2 мөр болбол ердөө **5 БАГАНА** болж дэлгэцийн тэн хагас хоосон.

**Зөв:** `Math.ceil(n / 2) >= perRow` — 2 мөр нь дэлгэцийг
**ДҮҮРГЭДЭГ** бол л утгатай.

```
perRow = floor((clientWidth + gap) / (cardWidth + gap))
cols   = ceil(items.length / 2)
twoRows = prev ? cols >= perRow - 1 : cols >= perRow   // гистерезис
```

Бодит дата (1920px, perRow=9):
| Эгнээ | Кино | Багана | Мөр |
|---|---|---|---|
| Шилдэг кино | 11 | 6 | 1 (гүйлгэнэ) |
| Хятад болон Ай | 24 | 12 | 2 |

⚠️ Гистерезис (`perRow - 1`) — 2 мөр болоход grid болж картын
хэмжээ өөрчлөгдвөл 1↔2 хооронд хязгааргүй сэлгэхээс сэргийлнэ.

---

## 19. Дансны захиалгын idempotent — `bankClaimedAt: null` ЗААВАЛ

⚠️⚠️ 24 цагийн idempotent нь **мэдэгдсэн** захиалгыг ч дахин
ашигладаг байв. Үр дүнд: хэрэглэгч «Шилжүүлсэн» дараад ДАХИН
«Дансаар шилжүүлэх» дарвал модал шууд «Хүлээн авлаа» дэлгэц
харуулж, **шинэ гүйлгээ хийх боломжгүй**.

Засвар: `where: { ..., bankClaimedAt: null }` — зөвхөн хараахан
мэдэгдээгүй захиалгыг дахин ашиглана.

Тест: `822602` → claim → дахин initiate → `916754` (шинэ утга).

---

## 20. Баримт эсвэл баталгаажуулалт — аль нэг ЗААВАЛ

⚠️ Товч шууд ажилладаг байсан тул хэрэглэгч санамсаргүй дараад
«мэдэгдсэн» төлөвт орж, өөрчлөх боломжгүй болдог байв.

Одоо: **баримтын зураг ЭСВЭЛ «Би төлбөрөө шилжүүлсэн» checkbox**.
Аль нь ч байхгүй бол товч **идэвхгүй** (саарал).

⚠️ Checkbox зам нь ЗААВАЛ хэрэгтэй — ATM, теллэр, өөр хүнээр
гүйлгүүлсэн хүн screenshot авч чадахгүй.

⚠️ Баримтгүй тохиолдолд:
- Telegram-д `⚠️ БАРИМТГҮЙ` гэж тэмдэглэгдэнэ
- Админ хүснэгтэд **шар** өнгөөр (саарал биш) ялгарна

⚠️ Админ «баримт заавал» гэж тохируулбал checkbox хангалтгүй.
