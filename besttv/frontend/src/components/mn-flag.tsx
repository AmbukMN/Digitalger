/**
 * Монголын төрийн далбаа — жижиг хэмжээний SVG icon.
 *
 * ⚠️⚠️ ЯАГААД ЭМОДЖИ БИШ ВЭ:
 * `🇲🇳` эмоджи нь Windows дээр далбаа болж РЕНДЕРЛЭГДДЭГГҮЙ — оронд нь
 * "MN" гэсэн 2 үсэг харагддаг (Microsoft үндэсний далбааны эмоджиг
 * зориудаар дэмждэггүй). Хэрэглэгч "MN гэж бичиг гарч байна" гэж
 * гомдоллосон нь яг энэ. SVG нь БҮХ төхөөрөмж дээр ижил харагдана.
 *
 * Пропорц: албан ёсны 1:2 харьцаа, 3 босоо талбар (улаан-хөх-улаан),
 * зүүн улаан талбарт соёмбо (энгийн хэлбэрээр).
 */
export function MnFlag({ className = 'h-3 w-[1.125rem]' }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 12 8"
      className={className}
      aria-hidden
      focusable="false"
      preserveAspectRatio="xMidYMid meet"
    >
      {/* Улаан — зүүн ба баруун талбар */}
      <rect width="4" height="8" fill="#C4272F" />
      <rect x="4" width="4" height="8" fill="#015197" />
      <rect x="8" width="4" height="8" fill="#C4272F" />
      {/* Соёмбо — алтан өнгө, зүүн улаан талбарын төвд (энгийн хэлбэр) */}
      <g fill="#F9CF02" transform="translate(2 4)">
        {/* Гал (дээд гурвалжин) */}
        <path d="M0 -2.9 L0.45 -2.05 L0 -2.3 L-0.45 -2.05 Z" />
        {/* Нар (дугуй) ба сар (хавирган) */}
        <circle cy="-1.55" r="0.3" />
        <path d="M-0.62 -1.15 a0.42 0.42 0 1 0 0.42 0.42 a0.32 0.32 0 1 1 -0.42 -0.42 Z" />
        {/* Дээд ба доод сум */}
        <path d="M-0.3 -0.62 h0.6 l-0.3 0.5 Z" />
        <path d="M-0.3 2.05 h0.6 l-0.3 -0.5 Z" />
        {/* Тэгш өнцөгт хос (баланс) */}
        <rect x="-0.85" y="-0.32" width="1.7" height="0.26" />
        <rect x="-0.85" y="1.16" width="1.7" height="0.26" />
        {/* Хос загас (төв) */}
        <rect x="-0.85" y="0.12" width="1.7" height="0.9" rx="0.12" />
        {/* Хос багана */}
        <rect x="-1.5" y="-0.62" width="0.26" height="2.06" />
        <rect x="1.24" y="-0.62" width="0.26" height="2.06" />
      </g>
    </svg>
  );
}
